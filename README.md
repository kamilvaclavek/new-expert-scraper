# New Expert Scraper

### Prerequisities

- Node.js
- Apify CLI

### Development

```bash
npm i #install dependencies
npm run build #build the project
apify run #run locally
```

### Deployment

The actor is configured to automatically build and deploy on a new commit to the main branch
